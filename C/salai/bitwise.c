#include<stdio.h>
int main()
{
	int a=1,b=3;

		printf("a=%d\t b=%d\n", a , b );
		printf("a & b output = %d \n", a & b );
		printf("a | b output = %d \n", a | b );
		printf("~ a output = %d \n", ~a );
		printf("a ^ b output = %d \n", a ^ b );

}
